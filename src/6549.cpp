#include<iostream>
#include<vector>
using namespace std;

int main(){

    int N;
    cin>>N;

    while(N != 0){

        vector<long long> arr;
        for(int i{0}; i < N; i++){
            long long num;
            cin>>num;
            arr.push_back(num);
        }

        
    }

    return 0;
}