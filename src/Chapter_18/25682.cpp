#include<iostream>
#include<vector>
#define sync_with_stdio(false)
using namespace std;

int main(){

    cin.tie(NULL); cout.tie(NULL);
    int N, M, K;
    cin>>N>>M>>K;

    vector<int>row(M, 0);
    vector<vector<int>>board(N, row);

    for(int i{0}; i < N; i++){
        for(int j{0}; j < M; j++){
            char c; cin >> c;

        }
    }

    return 0;
}